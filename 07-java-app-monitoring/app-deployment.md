# Java Application Monitoring

git clone https://github.com/Abinaya0106/kubernetes-java-deployment cd
kubernetes_java_deployment/Kubernetes

kubectl apply -f shopfront-service.yaml

Verify: kubectl get deployments kubectl get pods kubectl logs -l
app=shopfront

Monitored CPU and Memory metrics via Grafana dashboard.
